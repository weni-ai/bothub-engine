language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install python-coveralls
  - pip install flake8
env:
  - SECRET_KEY=SK
script:
  - flake8
  - coverage run manage.py test
  - coverage report
after_success:
  - coveralls
